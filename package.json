{
  "name": "@webcarrot/json-bc",
  "version": "0.0.1",
  "description": "Json like transport format - binary and compression included",
  "main": "dist/mod.cjs",
  "module": "dist/mod.mjs",
  "types": "dist/mod.d.ts",
  "type": "module",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "clean": "rm -rf ./dist",
    "prepack": "npm run build",
    "postpack": "npm run clean",
    "build:ts": "rollup -c ./rollup.config.mjs",
    "build:cjs": "rollup --format=cjs --file=dist/mod.cjs -p typescript -m -- src/mod.ts",
    "build:mjs": "rollup --format=es --file=dist/mod.mjs -p typescript -m -- src/mod.ts",
    "build": "npm run clean && npm run build:ts && npm run build:cjs && npm run build:mjs",
    "test": "tsc --noEmit && jest",
    "coverage": "jest --coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/webcarrot/json-bc.git"
  },
  "keywords": [
    "json",
    "binary",
    "compression",
    "transport"
  ],
  "author": "Wojciech Wierchoła",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/webcarrot/json-bc/issues"
  },
  "homepage": "https://github.com/webcarrot/json-bc#readme",
  "devDependencies": {
    "@rollup/plugin-typescript": "^8.3.3",
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "prettier": "^2.7.1",
    "rollup": "^2.77.0",
    "rollup-plugin-dts": "^4.2.2",
    "ts-jest": "^28.0.7",
    "ts-node": "^10.9.1",
    "tslib": "^2.4.0",
    "typescript": "^4.7.4"
  }
}
